[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "libxml2", "libxslt", "gcc"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt"]

[start]
cmd = "python3.11 -m gunicorn main:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
